var c = 1
    //Executa primeiro e depois faz o teste
    do {
        console.log(`Passo ${c}`)
        c++ // c = c + 1
    } while (c <= 6)

    // Testa Primeiro e depois executa
    var c = 1
    while (c <= 3 ) {
        console.log(`Passo ${c}`)
        c++ // c = c + 1
    } 